{
    "configurations": [
        {
            "type": "lldb",
            "request": "launch",
            "name": "Launch",
            "program": "${workspaceFolder}/cub3D",
            "args": ["maps/cub.cub"],
            "cwd": "${workspaceFolder}"
        }

    
    ]
}